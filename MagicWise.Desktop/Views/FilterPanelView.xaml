<UserControl x:Class="MagicWise.Desktop.Views.FilterPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MagicWise.Desktop.ViewModels">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding FilterPanel.Categories}">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:TagCategoryViewModel}">
                    <Expander Header="{Binding Name}"
                              IsExpanded="True"
                              Margin="0,2,0,2"
                              Visibility="{Binding IsVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                        <ItemsControl ItemsSource="{Binding Tags}" Margin="12,4,0,4">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:TagViewModel}">
                                    <CheckBox Content="{Binding Name}"
                                              IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                              Margin="0,2,0,2" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Expander>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
